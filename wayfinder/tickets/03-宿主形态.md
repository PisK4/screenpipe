# T3 · Intent Agent 宿主形态

Labels: wayfinder:grilling (HITL)
Blocked by: [T2 · 数据映射调研](02-数据映射调研.md)
Claimed by: pis（本会话，2026-08-22）

## Question

生成任务嵌进 fork 的哪个现成机制？候选都在仓库里现成：pipes 的 ScheduleConfig 周期调度、src-tauri 侧 tokio 循环 + ROUTE-DIRECT-CHAT 单次模型调用（见 `doc/FEATURE_MODEL_INVOCATION_INVENTORY.md`）、起一个 Pi 子进程会话。判据是「最大复用 fork 现成件、最少新建基础设施」，不是满足 Cue 合同的会话形态；合同里的工具白名单、中止边界只在选定形态后当作参照检查一遍。重写一张卡 = 再跑一轮同样的生成，是否需要独立机制也在此确认。

## Resolution

Status: Resolved
Claimed by: pis（2026-08-22，与用户逐项裁决）

### 决议：候选 B——Rust 侧心跳 + 单次直调，两层结构

1. **排除 pipes**：pipes 是第三方扩展面（用户可装卸的插件），核心产品功能不挂在它的生命周期上。
2. **选定 B**：Rust 侧定时器到点后，查增量信号 → 内部调 `/activity-summary` 拿有界摘要 → 拼 prompt → 一次 OpenAI 兼容直调（local-first 默认 Ollama，provider 细节归 T7）→ 解析 JSON 方案数组 → 落 `intent_cards` → 发事件给前端。无会话、无工具循环。
3. **节拍与判据分离**（响应用户「不断累积评估、不是到点就出卡」的定调）：心跳只有观察权没有出卡权。每次醒来拉增量信号（`ui_events` 的 app_switch/window_focus 计数、`frames` 的 simhash/content_hash 变化），过自写规则门槛才发起生成调用；生成调用本身仍允许模型回答「材料不足」。阈值数值是实现期调参项，不是本图决策。
4. **上下文累积 = 查询窗口增长**：数据已由采集引擎持续落库，「攒上下文」实现为查询窗口从上一张卡时刻滚到现在，不新建任何存储组件；跨窗持久摘要将来若需要再议（见地图雾区 Daily Summary 持久化存档）。
5. **常驻语义**：只要采集引擎在跑就生成，不要求主窗口打开（托盘挂窗口关算数）。调度器住 Rust 侧，与引擎同生命周期；引擎完全停止则无材料可生成，属物理边界。具体宿主进程（Tauri 托盘进程 vs 独立守护）实现期定。
6. **新手卡旁路**：新增一个 Tauri command 手动触发生成→落库→推送，跳过心跳与判据——即 T4 定的开发期测通载具。
7. **升级路径**：卡片质量不够时，把「拼简报→单次调用」一段换成 Pi 子进程会话（`daily-summary-pi.ts` 的渐进披露模式为先例）；表结构、触发层、展示层均不动。重写已在 T4 砍掉，不存在 agent 重入需求。

### 对 T7 的输入

直调形态走 OpenAI 兼容接口，Ollama 基线能否胜任、BYOK 引导、失败记账都在 T7 裁决。

### 修订记录（2026-08-23，HITL，用户逐项裁决）

**修订：生成段由单次直调改为 Pi 子进程会话，提前到 v1 落地。** 决议 1/3/4/5/6（排除 pipes、节拍判据分离、查询窗口、常驻语义、新手卡旁路）全部不变；被替换的是决议 2 的「拼 prompt → 单次 OpenAI 兼容直调」一段与决议 7 的升级路径定位。

1. **动机是产品目标变了，不是架构偏好。** 用户给 v1 定的标准是 aha moment——挖出用户自己没意识到的模式、主动给出下一步 action。这要求模型能越过 activity-summary 的固定聚合视角自己去查证数据；单次直调下这个天花板是结构性的。同时把 Pi 基础设施一次付清后，后续加工具只是改白名单配置，不再有架构改动。
2. **会话形态照 `daily-summary-pi.ts` 先例**：门槛过后起一个专用 projectDir 的 Pi 会话，把摘要、窗口信号与指令放进去，取会话终报解析。输出契约不变——终报必须是带 `"v":1` 的 JSON 或 `{"insufficient_material":true}`，parse 层原样复用；会话超时或格式不符记 tracing 失败，心跳下轮重试，不建双路径回退。
3. **工具面第一天就收窄为只读白名单**（全量继承是 Pi 默认行为，必须显式覆盖）：候选 sp_mcp_list_tools / sp_mcp_call 等 Cue 数据读取类工具；禁 bash 与一切写侧工具——无人值守的定时任务不得持有任意执行能力。精确清单实现期对照 `pi.rs` 的 allowlist 机制定。
4. **skills 只进基线集**（core 现状为 screenpipe-api / screenpipe-cli / render-html-report 三件；票面初稿写的 `-team` 已被 core 作为 legacy 删除，以 core `BASELINE_SKILL_NAMES` 为准）：教模型怎么查本地数据，服务挖掘目标。用户自装的 skills 不镜像进意图会话——无人值守周期性执行用户导入的任意指令，风险性质与 chat 里人盯着跑不同；将来可做成开关再议。
5. **对 T7 的新输入**：供给对象从 endpoint 三元组变为 preset→PiProviderConfig 映射；BYOK 引导随本次修订进 v1（见 T7 修订记录）。
6. 新手接入卡仍是确定性旁路，不起会话不走模型（D5 不变）。
