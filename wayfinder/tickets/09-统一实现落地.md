# T9 · 统一实现落地

Labels: wayfinder:task (AFK)
Blocked by: [04-save_intent_draft机制裁决](04-save_intent_draft机制裁决.md)、[06-完整生成流程文本定稿](06-完整生成流程文本定稿.md)、[07-文案风格定稿](07-文案风格定稿.md)
Claimed by:

## Question

地图 Notes 的既定纪律：所有决策票走完之前不写生产代码。本票在 T4/T6/T7 全部定稿后，把已入账的全部裁决**一次性**落进代码与文档，避免同一批文件被反复改动：

1. 多卡链路（T3 裁决）：session 收集全部 `submit_intent_card` 调用并统计 `save_intent_draft` 次数；runner 循环落库、单拍上限 2、同批同名去重、状态四分类映射；instruct 任务行与 system prompt 措辞放宽；intent-card.ts description 放宽；相关测试同步；
2. api_hint 写进 `build_system_prompt()` 文本（T5 结论：所有模型注入，只点名 cue-tools）；
3. 流程文本按 T6 定稿写入 system prompt、cue-tools §2、payload `[OPEN_DRAFTS]` 三处，并同步 `doc/FEATURE_INTENT_CARDS.md`；
4. 文案按 T7 定稿的风格规范落到卡片字段与 i18n 字典；
5. 草稿机制按 T4 裁决落地 wrapper 硬护栏与数值；
6. 测试跑最窄边界（src-tauri 按 AGENTS.md 指引显式测试；pre_build 后记得还原 `gen/schemas/`）。

代码注释必须自含（根 `AGENTS.md`），禁止引用票据编号。

## Resolution

Status: Open
